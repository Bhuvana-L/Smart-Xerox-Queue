{
  "name": "smart-xerox-queue",
  "version": "1.0.0",
  "description": "Smart Xerox Queue Management System",
  "scripts": {
    "build": "cd backend && npm install",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
